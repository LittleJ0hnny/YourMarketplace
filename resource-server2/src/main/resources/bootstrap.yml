spring:
  cloud:
    config:
      name: resource-server2
      discovery:
        service-id: configserver
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka